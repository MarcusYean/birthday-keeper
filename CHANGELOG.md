# 更新日志 (Changelog)

## v2.6.18 (2026-07-28)

- 🔔 **每人提醒天数独立、移出个人资料**：每个人的「提前提醒天数」本就在各自联系人 / 纪念日中单独设置，现已正式作为提醒的唯一驱动（系统按每条记录的 `notify_days` 判断，未设置则回退全局默认），个人资料中不再重复设置提前天数。每人编辑框的提醒天数选择器新增「当天」选项（即第 0 天，生日 / 纪念日当天也提醒）。
- 📥 **「我的提醒设置」精简为三种渠道**：个人资料及偏好页的提醒方式只保留「站内信 / 微信 / 飞书」三种；勾选「微信」下方展开其推送配置（服务类型、Token、Bark 服务器），勾选「飞书」下方展开机器人配置（Webhook、签名密钥），勾选「站内信」提示通过站内信收件箱接收。邮件、Webhook 渠道已移除。
- 🔧 **微信 / 飞书凭据下沉到个人资料**：发送微信、飞书提醒时优先使用「个人资料及偏好」中每个人自己填写的凭据，未填写时回退全局配置。管理员设置中原有的「微信推送 / 飞书机器人 / 邮件通知」分组已移除，提醒策略以个人资料设置为主。
- 🧩 **「个人资料」合并「偏好」**：左侧导航与用户菜单的「个人资料」「偏好」两项合并为「个人资料及偏好」；主题与语言切换已移入该页面，不再作为独立入口。
- 🔄 静态资源版本号升级到 `?v=2.6.18`。

## v2.6.17 (2026-07-27)

- 🐛 **修复顶部栏菜单 + 非默认主题布局错乱**：桌面主题（warm/mint/morandi/sakura）对 `html[data-theme] .sidebar` 定义了 `width:236px` 等纵向样式，与顶部栏模式 `body.menu-top .sidebar { width:100% }` 同特异性、且源码靠后而胜出，导致顶部栏被压成窄竖条。现已用更高特异性（并限定桌面宽度）在末尾重新声明横向布局，仅「极简现代」可用的顶部栏在所有主题下均正常。
- 🏷️ **列表标签同行不换行**：联系人/纪念日列表的「启用」「私人/家庭」等状态标签原先因 `flex-wrap: wrap` 在名称较长时会掉到第二行。已将 `.name-line` / `.row-status` 改为 `nowrap`，并把关系/类型副文本并入同一行，确保「一个人/一个纪念日就是一行」。
- 🎨 **统一个人资料昵称/邮箱编辑框**：CSS 原先只命中 `input[type=text/number/password]`，导致 `type="email"` 与未声明 type 的昵称输入框呈现浏览器原生样式、与用户名框不统一。已将选择器放宽为 `.field input:not([type=checkbox]):not([type=radio])`，并为昵称输入补 `type="text"`，三者视觉完全一致。
- 🔄 静态资源版本号升级到 `?v=2.6.17`。

## v2.6.16 (2026-07-27)

- 🎨 **统一个人资料编辑框 UI**：个人资料页「用户名（不可修改）」输入框原先未声明 `type="text"`，导致 `.field input[type=text]` 样式未命中，呈现浏览器默认禁用样式、与昵称/邮箱输入框不统一。现已统一并优化禁用态视觉。
- 🐛 **修复切换主题后导航布局变形**：移动端底部导航的 `.nav-item` 竖排样式未限制在底部/顶部菜单模式下，当窗口宽度 ≤720px 时桌面侧边栏的导航项也会被错误地压缩成图标+竖排文字。现已把相关样式限定在 `body.menu-left` / `body.menu-top` 的移动端场景，桌面侧边栏恢复正常的横向图标+文字。
- 🖱️ **列表行改为点击名称编辑**：联系人/纪念日列表（列表/卡片/紧凑三种视图）不再显示每行的「编辑」「删除」按钮，点击名称/头像区域即可打开与「添加」相同的编辑抽屉；编辑抽屉底部新增「删除」按钮，确认后关闭抽屉并刷新列表。纪念日列表同理。
- 👁️ **即将到来改为只读完整信息**：「即将到来」视图移除所有编辑/删除操作按钮，列表模式新增「关系/类型」「日历」「备注」列，卡片模式显示关系/类型与备注，确保每条信息完整可读。
- 📊 **看板新增饼图与标签云**：在现有条形图基础上，数据分析区新增「关系占比」SVG 饼图、「性别占比」SVG 饼图，以及「星座云图」「生肖云图」标签云，5 主题兼容、响应式适配。
- 🔄 静态资源版本号升级到 `?v=2.6.16`。

## v2.6.15 (2026-07-27)

- 🐛 **修复闰月生日无法计算下一个生日**：`app/lunar.py` 的 `_lunar_to_solar` 在「闰月且当年无该闰月」时（如「闰五月」遇无闰月之年）原会整体失败返回 `None`，导致 `next_occurrence` 返回 `None`、界面显示「算不出来」。现已增加回退逻辑：当年无该闰月时，自动回退到同月的普通版本（民间惯例：无闰月年份在农历同月过），闰月生日的下一个公历日期可正常计算。
- 🐛 **修复主题切换后看板布局变形**：v2.6.14 新增的「数据分析」样式（时间线 `.dash-tl`、洞察 `.insight-list` 等）被错误嵌套进 `@media (max-width: 720px)` 移动端媒体查询内，导致桌面端这些样式完全不生效、时间线柱子在桌面端竖向堆叠错乱。现已将分析区块样式移出媒体查询（桌面 / 移动端通用），时间线恢复为正常的横向柱状图。
- 🔄 静态资源版本号升级到 `?v=2.6.15`。

## v2.6.14 (2026-07-27)

- 📊 **看板数据可视化与分析**：首页看板新增「数据分析」区块——未来 30 天生日时间线（迷你柱状图）、月度生日分布、年龄段分布、关系分布，以及自动生成的关键洞察（生日最集中月份、未来 30 天数量、人数最多星座、平均年龄）。
- 🌐 **修复看板标记显示不完整**：补全 `viewTitle.dashboard` 等缺失国际化键，统一性别存储/显示为 key 处理（`genderKey` / `genderLabel`），看板不再出现 `viewTitle.dashboard`、`gender` 等原始键名。
- ☑️ **全选框语义清晰化**：列表表头全选框明确为「全选本页」，与工具栏「全选」（跨页全部）、分组「全选本组」形成「全部 / 本页 / 本组」三层清晰分工，消除歧义。
- 📋 **列表单行完全显示**：列表改为自适应布局，所有内容强制单行（`white-space: nowrap`）并在容器横向滚动中完整呈现，不再被截断。
- 🎨 **统一输入框样式**：个人资料等页面的输入框统一标签与输入框间距，补齐全选/禁用等状态样式，整体更协调。
- ⚙️ **通知设置综合评估与优化**：保留系统级调度参数（检查时间）、默认提前天数 / 渠道、各渠道凭证与消息模板（这些是系统默认值，个人在「个人资料 → 我的提醒」单独设置时优先），并在通知设置组新增说明提示，避免误以为冗余。
- 🧭 **用户名 hover 下拉菜单**：顶栏用户名（hover 或点击）展开快捷菜单，直达「个人资料 / 偏好 / 设置」，并含「退出」；移动端改为点击展开。
- 🔄 静态资源版本号升级到 `?v=2.6.14`。

## v2.6.13 (2026-07-26)

- 🪪 **账号与昵称分离**：每个用户拥有独立「个人资料」页（导航新增 🪪 个人资料）。账号（用户名）保持唯一，仅用于登录与家庭邀请；昵称可自由修改、允许重复，页面顶栏和界面统一显示昵称。个人资料页可修改昵称、联系邮箱，并可在校验当前密码后修改登录密码（新密码至少 6 位）。
- ☑️ **修复分组全选范围错误**：联系人按分组显示时，每个分组标题旁新增该组专属的全选框，勾选后**只选中本组人员**（此前会误选所有人员）；「本页全选」与各组全选框支持半选状态并实时联动。
- 🔔 **用户级独立提醒设置**：提醒设置从全局迁移到每个用户的「个人资料 → 提醒设置」，各用户完全独立、互不影响。三步极简设置：① 勾选提醒方式（站内信 / 邮件 / Webhook / 企业微信 / 飞书）→ ② 勾选提前天数（当天 / 1 / 3 / 7 / 15 天，可多选）→ ③ 可选自定义提醒内容模板（支持 `{name}`、`{days_until}`、`{age}`、`{date}` 等变量，留空使用默认文案）。邮件渠道可填写个人收件邮箱，Webhook 渠道可填写个人接收地址。
- 📬 **新增站内信收件箱**：顶栏新增 🔔 铃铛与未读数徽章，提醒到期时按每个用户的可见范围（本人 / 家庭共享 / 公开）独立投递站内通知；支持单条已读与一键全部已读。私密记录只提醒本人，不会泄漏给其他用户。
- 🔗 **新增通用 Webhook 提醒渠道**：向用户自定义 URL POST `{"title", "content"}` JSON，方便接入自建机器人或第三方服务。
- ⚙️ 调度器重构为「记录 × 可见用户」逐人投递：每个用户按自己的渠道、提前天数、内容模板独立触达，并按（用户, 记录, 年份, 提前天数）独立去重，互不干扰。
- 🌐 新增文案已覆盖简体 / 繁體 / English / 한국어 / 日本語 五种语言（每语言 +47 键）。
- 🔄 静态资源版本号升级到 `?v=2.6.13`。

## v2.6.12 (2026-07-26)

- 🏠 **新增「首页」看板**：登录后默认进入首页，一屏总览——联系人 / 纪念日总数、30 天内生日、今天过生日、今年已过生日 5 张统计卡片，加上「最近的生日」「最近的纪念日」列表与星座 / 生肖 / 性别分布条形图；提供「刷新看板」按钮实时重算。
- 🔍 **综合搜索**：联系人、纪念日、即将到来三个页面均新增搜索框，联系人可按姓名 / 关系 / 性别 / 星座 / 生肖 / MBTI / 血型 / 爱好 / 备注 / 历法等所有可搜索字段模糊匹配；纪念日按名称 / 类型 / 说明；即将到来按名称 / 关系 / 类型。
- 🧰 **组合筛选**：联系人支持性别 + 星座 + 生肖 + 历法 + 提醒状态五个条件任意组合；纪念日支持历法 + 提醒状态；即将到来支持类别（生日 / 纪念日）+ 历法。筛选按钮上有生效条件数量徽章，可一键重置。
- 📏 **分组表格列宽统一**：列表视图改用 `colgroup` + `table-layout: fixed` 固定列宽，分组 / 分页产生的多个表格列宽完全一致，不再参差不齐（窄屏堆叠布局不受影响）。
- ⏱ **新增「已活天数」排序**：联系人排序下拉新增「已活天数 ↑ / ↓」。
- 🔄 **刷新重算**：「即将到来」新增刷新按钮；联系人 / 纪念日 / 首页的刷新按钮均会重新请求接口，由后端实时重算下次生日、倒计时、已活天数、年龄、星座、生肖等自动计算字段。
- 🐛 **修复「即将到来」纪念日类型显示英文 anniversary**：后端此前把纪念日用户填写的类型（kind）覆盖为英文 "anniversary"，现改用独立 `category` 字段区分生日 / 纪念日，类型标签恢复显示用户填写的原文。
- 🌐 语言包懒加载 URL 追加版本号，发版后不再吃旧缓存导致新文案缺失；界面新增文案已覆盖简体 / 繁體 / English / 한국어 / 日本語 五种语言。
- 🔄 静态资源版本号升级到 `?v=2.6.12`。

## v2.6.11 (2026-07-27)

- ♌ **阴历生日按出生年份算星座**：此前阴历生日误用「今年/明年的公历对应日」推算星座，年份与月日都可能与实际出生年不符。现改为：阴历生日先用出生年份把阴历转换为对应的阳历日期，再按阳历计算星座（缺出生年份时退回到今年的公历对应日作近似兜底）。已用多组阴历生日校验。
- 🔄 静态资源版本号升级到 `?v=2.6.11`，确保浏览器刷新后加载新样式。

## v2.6.10 (2026-07-27)

- ♈ **修复星座计算错位**：此前自动计算西方星座时逻辑错误，每个日期都会整体「后错一个星座」（例如 1 月 1 日误判为水瓶座、3 月 25 日误判为金牛座）。修正为「取最后一个起始日 ≤ 生日的星座」，已对全部 12 个星座的交界日逐一校验通过。
- 🔄 静态资源版本号升级到 `?v=2.6.10`，确保浏览器刷新后加载新样式。

## v2.6.9 (2026-07-27)

- 📅 **纪念日去「人化」**：纪念日本质是日期而非人物，表单不再显示「姓名 / 关系」，改为「纪念日名称」+「纪念日的说明」，列表也相应展示名称与说明。
- 🗓 **即将到来改为「全年视图」**：「即将到来」页现在展示当前自然年内的**全部**日期，并拆分为「即将到来」与「今年已过」两组；同时支持与联系人一致的**列表 / 卡片 / 紧凑**三种视图模式（选择记忆在本地）。

## v2.6.8 (2026-07-27)

- 🎨 **弹窗四角统一圆润**：修复桌面端弹窗右上、右下角显示为直角的视觉问题。通过把滚动区域收进 `.modal-inner`/`.drawer-inner`，外层 `.modal`/`.drawer` 只负责圆角与裁切，使四个角都与左侧保持一致圆润。
- 🐉 **新增「属相」自动计算**：在添加 / 编辑联系人表单中增加「属相」只读字段，根据填写的出生年份自动计算 12 生肖，年份变化时实时更新。
- 📋 **提前提醒天数改为多选项**：联系人、纪念日表单以及设置页的「默认提前提醒天数」均由文本输入改为 1 / 2 / 3 / 5 / 7 / 10 / 15 / 30 天的胶囊式多选，选择更直观，也避免手动输入格式错误。
- 🔄 静态资源版本号升级到 `?v=2.6.8`，确保浏览器刷新后加载新样式。

## v2.6.7 (2026-07-26)

- 🏠 **家庭可重命名 / 删除 / 移除成员 / 退出**：此前创建的家庭既改不了名字也删不掉。本版新增家庭管理操作——创建者或管理员可重命名、删除家庭、将成员移出；普通成员可一键退出自己所在的家庭（创建者不可被移出/退出，需先删除家庭）。
- 🔐 **权限分离（管理员 vs 普通用户）**：
  - **管理员**：可见全部菜单（联系人 / 纪念日 / 即将到来 / 家庭 / 偏好 / 设置 / 用户管理），可查看与修改系统内所有联系人 / 纪念日数据，且可见范围额外拥有「公开（系统内所有用户可见）」选项。
  - **普通用户**：只看到与自己相关的菜单（无 设置 / 用户管理），只能查看 / 编辑自己的数据 + 同家庭成员共享的数据，且新建 / 编辑时可见范围只有「私人 / 家庭」两项，无法设置为「公开」。
- 🔄 静态资源版本号升级到 `?v=2.6.7`，确保浏览器刷新后加载新样式。

## v2.6.6 (2026-07-26)

- 📱 **进一步优化手机端底部导航**：继续修复 v2.6.4 移动端底栏与页面不协调的问题。7 个导航入口在窄屏下文字过大导致换行（如「纪念日」「即将到来」「用户管理」折成两行），本版将底栏字号缩小到 9px（超窄屏 8.5px）、禁止文字换行并启用省略号，同时略微缩小图标与间距，让底栏在各主题下都保持一行、整齐、与页面协调。
- 🔄 静态资源版本号升级到 `?v=2.6.6`，确保浏览器刷新后加载新样式。

## v2.6.5 (2026-07-26)

- 📝 **新增「消息模板」设置**：在「设置 → 消息模板」中可自定义生日/纪念日提醒与测试通知的标题和正文。支持变量 `{name} {calendar_type} {month} {day} {year} {relationship} {note} {days_until} {age} {age_num} {years_together} {years_together_num} {when} {zodiac} {chinese_zodiac} {next_date} {kind}`，保存后立即生效，无需重启容器。
- ➕ **修复「创建家庭」按钮双加号**：HTML 中手写了一个 `+ `，而 i18n 值里又带了一个 `+`，导致按钮显示成 `+ + 创建家庭`。已去掉 HTML 中的多余加号。
- 🔒 **修复权限漏洞：owner_id 为空的记录被错误公开**：`_is_visible` 原把 `owner_id` 为 `NULL` 的记录视为公开，导致刚注册的用户也能看到他人标记为「家庭」或「私人」但无主的联系人。已改为：无主记录仅当 `visibility=public` 时才公开，其余情况均不可见。
- 🔄 静态资源版本号升级到 `?v=2.6.5`，确保浏览器刷新后加载新样式。

## v2.6.4 (2026-07-26)

- 🔧 **修复开关位置（呼应 v2.6.3 的修复）**：上一版为修「开关被拉长」误把开关用 `margin-left:auto` 顶到行尾最右侧，导致与左侧文本输入框错位、视觉割裂。本版去掉右推，开关回到控件列起始处、与文本输入框左对齐一致；并修复窄屏（≤680px）下开关被 `width:100%` 拉伸成整行宽的问题。
- 📱 **手机端底部导航按主题协调**：此前桌面主题里的侧边栏样式会「反向污染」移动端底部条（包豪斯底部条出现右侧黑色竖线、玻璃主题出现多余右边框等）。本版在 CSS 末尾按 5 套主题分别重写了移动端底部导航条（背景/边框/阴影/圆角）与导航项（背景/激活态/字体），并让弹窗、抽屉在移动端作为底部抽屉时的圆角跟随主题。
- 🔄 静态资源版本号升级到 `?v=2.6.4`，确保浏览器刷新后加载新样式。

## v2.6.3 (2026-07-26)

- 🎨 **5 套完全不同的主流 UI 风格主题**：
  - **Clean Modern（极简现代）**：默认，大量留白、低饱和冷色、柔和圆角与阴影，Apple 式极简。
  - **Glassmorphism（玻璃拟态）**：半透明磨砂卡片、模糊背景、渐变光影，按钮与输入框如玻璃层叠。
  - **Soft UI / Neumorphism（新拟态）**：从背景中凸起的柔和双色阴影、无硬边框、按压/凸起形态。
  - **Bauhaus / Brutalist（包豪斯）**：直角粗黑边框、强对比红黑、块面硬阴影、大写标签，几何感强烈。
  - **Material You**：大圆角、分层粉彩卡片、渐变胶囊按钮、鲜明阴影，Material Design 风格。
  每套主题都改变了字体、圆角、阴影、按钮、卡片、表格、标签、输入框、侧边栏、导航、开关、复选框的整体形态，不只是换色。
- 🧊 **修复开关被拉长的问题**：设置页布尔项现在使用 `.field-label` 显式标记文本标签，避免 `.switch` 标签被错误地赋予 150px 宽度而变成长条。
- ✅ **美化多选框**：将原来的原生小方框改为「胶囊/标签式自定义复选框」，带勾选动画，与主题风格统一，间距更合理。
- 🌐 **主题名称多语言化**：为每种语言新增 `theme.*` 键，偏好页主题名称会随界面语言实时翻译。
- 🔄 静态资源版本号升级到 `?v=2.6.3`，确保浏览器刷新后加载新主题与样式。



- 🎨 **5 套截然不同 UI 风格的完整主题**：
  - **Clean**：现代极简，锐利卡片 + 低饱和蓝紫
  - **Warm**：家庭相册/纸质感，米白纹理 + 衬线字体 + 照片边框
  - **Mint**：清新薄荷/胶囊风，全圆角药丸按钮 + 薄荷渐变
  - **Morandi**：艺术画廊/包豪斯，直角粗边框 + 扁平无阴影 + 大写标签
  - **Sakura**：樱粉浪漫/棉花糖，超大圆角 + 柔和渐变 + 糖果按钮
  每套主题在字体、圆角、阴影、按钮、卡片、表格、标签、输入框、侧边栏、导航 active 样式上均有独立设计语言。
- 📐 **设置页布局优化**：label 与选项框（select/switch）间距加大到 28px，控件不再贴字；移动端自动切换为上下布局。
- 🔄 静态资源版本号升级到 `?v=2.6.2`，解决浏览器缓存旧主题/旧布局问题。

## v2.6.1 (2026-07-26)

修复缓存与布局问题，主题升级为「完整主题」：

- **5 套完整主题（不再只是换色）**：clean（极简白，默认）/ warm（暖灰纸张）/ mint（清新薄荷）/ morandi（莫兰迪艺术）/ sakura（樱粉浪漫）。每套主题拥有独立的字体栈、圆角体系、阴影层次、按钮/卡片/标签/输入框风格，切换后整体视觉气质完全不同。
- **内容区真正自适应**：移除 `.content` 的 `max-width` 限制，表格外包 `.table-card` 实现横向滚动，避免列多时被挤压在左侧。
- **静态资源加版本号防缓存**：`styles.css` / `i18n.js` / `app.js` 均带 `?v=2.6.1`；后端对无版本号的 `/static` 文件返回 `no-cache`，带版本号则允许长期缓存，确保新镜像部署后浏览器立即生效。
- **偏好页渲染兜底**：若主题/语言数据未加载，提示用户 `Ctrl+F5` 强制刷新，避免空白。

## v2.6 (2026-07-25)

5 套简约主题 + 国际化（i18n）：

- **5 套简约主题（均为浅色极简风格）**：clean（极简白，默认）/ warm（暖灰）/ mint（薄荷）/ morandi（莫兰迪）/ sakura（樱粉）。通过 `html[data-theme=...]` 覆盖 CSS 变量实现，导航新增「偏好」页，可一键切换主题与语言，立即生效。
- **多语言（i18n）**：界面新增简体中文 / 繁體中文 / English / 한국어 / 日本語 五种语言。静态文本走 `data-i18n` 属性自动翻译；动态文本（列表字段、表单、Toast、确认框、设置 schema、用户/家庭/邀请文案、量词「岁/天/年/周年/今天」等）全部通过 `t()` 翻译函数渲染，切换语言时实时刷新当前视图。语言与主题各自保存在 `localStorage`，按用户偏好生效。
- **偏好页**：左侧导航新增「偏好」入口，集中展示「界面主题」与「语言」两套选择器，所选即时应用并持久化。

## v2.5 (2026-07-25)

登录修复 + 权限体系 + 家庭共享 + 全新极简白 UI：

- **修复登录 500 错误**：旧版本数据库缺少新列（如 `role`/`created_at`）时登录会直接报「请求失败 500」。现启动时自动检测并补齐所有缺失字段（通用列迁移），旧库无缝升级、无需手动处理。
- **数据可见范围（私人 / 家庭 / 公开）**：每条生日和纪念日都可以设置可见范围——🔒 私人仅本人可见；🏠 家庭对家庭成员可见；🌍 公开对所有用户可见。在「设置 → 隐私与注册」中可配置新数据的默认可见范围。
- **家庭共享**：新增「家庭」页面，可创建家庭并**按用户名邀请**其他用户；对方在「家庭」页看到邀请，接受后即成为家庭成员，双方可互见「家庭」权限的数据。待处理邀请数会在导航上以红点数字提示。
- **开放注册**：管理员在设置中开启「开放用户注册」后，登录页出现「注册账号」入口，用户可自助注册（普通用户角色）。
- **密码找回**：登录页新增「忘记密码」。系统已配置邮件渠道时会向配置的邮箱发送带链接的重置邮件（1 小时有效）；未配置邮件时提示联系管理员。管理员也可在「用户管理」中直接为任意用户**重置密码**。
- **UI 全面升级为「极简白」风格**：大面积留白、灰白基调、低饱和冷色点缀、更轻的字重与更柔和的阴影圆角；登录页、列表、卡片、设置、弹窗、抽屉全部翻新，移动端适配保持不变。
- 编辑联系人 / 纪念日时的可见范围改为直观的分段选择按钮；列表中显示可见范围徽章。

## v2.4 (2026-07-25)

移动端适配（手机访问大幅提升友好度）：

- **侧边栏 → 底部固定导航栏**：手机上导航变为 App 风格的底部标签栏（图标 + 文字），无论菜单设为「左侧」还是「顶部」都生效，并适配刘海屏安全区。
- **列表视图在手机上变堆叠卡片**：多列表格自动转为「字段名 : 值」的竖向卡片（联系人 / 纪念日 / 用户管理均适用），不再需要横向滚动。
- **工具栏自适应**：视图 / 排序 / 分类 / 每页 等下拉框在手机上铺满整行，按钮（添加 / 批量测试 / 显示字段等）整行排列，方便点按。
- **弹窗改为底部抽屉（bottom sheet）**：新建 / 编辑联系人在手机上从底部滑出全宽面板；右抽屉编辑同样全宽。
- **卡片视图单列、紧凑视图换行**：联系人卡片单列展示，紧凑模式操作按钮换行到第二行不再被挤压。
- **设置二级菜单横向滚动**：分组导航在手机上变为可横滑的胶囊标签。
- 登录卡片、Toast 提示均做了窄屏优化，超窄屏（≤420px）进一步压缩字号与间距。

## v2.3 (2026-07-25)

针对批量测试与信息提示的进一步优化：

- **MBTI/血型提示不再被截断**：悬停 ℹ️ 分析改为全局固定定位 tooltip，避免在弹窗 / 抽屉内部被 `overflow` 截断。
- **批量测试改为行内选择**：联系人列表每行增加复选框；工具栏新增「全选 / 反选 / 取消选择」按钮；先勾选联系人，再点「🔔 批量测试」直接测试已选项，无需在弹窗中翻找几百人。
- **每页数量可选**：联系人页支持每页 10 / 20 / 50 / 100 / 200 / 全部 六种分页，翻页控件与总数量显示常驻底部。
- 纪念日批量测试保留弹窗模式（纪念日通常数量较少）。

## v2.2 (2026-07-25)

界面与体验大幅增强，并新增「纪念日」模块：

- **菜单位置可切换**：在「设置 → 界面与外观」中选择导航放在**左侧栏**或**顶部栏**，保存即生效。
- **编辑方式可切换**：新增联系人 / 编辑支持**右侧抽屉**模式（保留原居中弹窗），在设置中切换。
- **批量测试**：移除每行单独的测试按钮，改在联系人 / 纪念日列表顶部提供「🔔 批量测试」，可勾选多人或全部发送测试通知。
- **设置二级菜单**：设置页按「界面与外观 / 提醒策略 / 微信 / 飞书 / 邮件 / 系统」分组，左侧导航快速跳转。
- **自定义头像**：每个联系人可**上传照片**作头像（PNG/JPG/WEBP，≤2MB），未上传时回退到 emoji 头像。
- **纪念日模块**：新增「纪念日」类型（结婚 / 恋爱 / 入职 / 毕业…），同样支持提前提醒与「已携手 N 年」统计；「即将到来」页合并展示生日与纪念日。可在设置中关闭该入口。
- **MBTI / 血型字段**：联系人新增 MBTI 与血型；在列表 / 卡片 / 紧凑视图中带 ℹ️ 图标，**悬停显示性格与健康小贴士**。
- **GitHub Releases**：以 `vX.Y.Z` 形式发布版本，每次发版同时构建并推送版本固定镜像标签 `:vX.Y.Z`，便于锁定版本。

## v2.1 (2026-07-23)

联系人卡片化与视图增强：

- 列表 / 卡片 / 紧凑三种视图；按名称 / 倒计时 / 年龄 / 创建时间排序；按关系 / 性别 / 星座 / 生肖 / 历法 / 生日月份分组。
- 列表自动展示下次生日、倒计时、年龄、已活天数；「显示字段」选择器自定义展示列。
- 新增性别、出生时辰、星座、爱好、emoji 头像字段。

## v2.0 (2026-07-22)

现代化重构 + 多用户 + 全参数前台可调：

- 现代化简约 UI；多用户（管理员 / 普通用户）。
- 管理员可在网页「设置」页调整全部参数，**每项参数附说明**，保存即热生效、无需重启。
- 农历 / 公历双历法、微信 / 飞书 / 邮件多渠道通知。
- GitHub Actions 自动构建并发布到 GHCR，支持「零克隆零构建」直接拉镜像部署。
