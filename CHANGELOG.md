# 更新日志 (Changelog)

## v2.4 (2026-07-26)

移动端适配（手机访问大幅提升友好度）：

- **侧边栏 → 底部固定导航栏**：手机上导航变为 App 风格的底部标签栏（图标 + 文字），无论菜单设为「左侧」还是「顶部」都生效，并适配刘海屏安全区。
- **列表视图在手机上变堆叠卡片**：多列表格自动转为「字段名 : 值」的竖向卡片（联系人 / 纪念日 / 用户管理均适用），不再需要横向滚动。
- **工具栏自适应**：视图 / 排序 / 分类 / 每页 等下拉框在手机上铺满整行，按钮（添加 / 批量测试 / 显示字段等）整行排列，方便点按。
- **弹窗改为底部抽屉（bottom sheet）**：新建 / 编辑联系人在手机上从底部滑出全宽面板；右抽屉编辑同样全宽。
- **卡片视图单列、紧凑视图换行**：联系人卡片单列展示，紧凑模式操作按钮换行到第二行不再被挤压。
- **设置二级菜单横向滚动**：分组导航在手机上变为可横滑的胶囊标签。
- 登录卡片、Toast 提示均做了窄屏优化，超窄屏（≤420px）进一步压缩字号与间距。

## v2.3 (2026-07-25)

针对批量测试与信息提示的进一步优化：

- **MBTI/血型提示不再被截断**：悬停 ℹ️ 分析改为全局固定定位 tooltip，避免在弹窗 / 抽屉内部被 `overflow` 截断。
- **批量测试改为行内选择**：联系人列表每行增加复选框；工具栏新增「全选 / 反选 / 取消选择」按钮；先勾选联系人，再点「🔔 批量测试」直接测试已选项，无需在弹窗中翻找几百人。
- **每页数量可选**：联系人页支持每页 10 / 20 / 50 / 100 / 200 / 全部 六种分页，翻页控件与总数量显示常驻底部。
- 纪念日批量测试保留弹窗模式（纪念日通常数量较少）。

## v2.2 (2026-07-25)

界面与体验大幅增强，并新增「纪念日」模块：

- **菜单位置可切换**：在「设置 → 界面与外观」中选择导航放在**左侧栏**或**顶部栏**，保存即生效。
- **编辑方式可切换**：新增联系人 / 编辑支持**右侧抽屉**模式（保留原居中弹窗），在设置中切换。
- **批量测试**：移除每行单独的测试按钮，改在联系人 / 纪念日列表顶部提供「🔔 批量测试」，可勾选多人或全部发送测试通知。
- **设置二级菜单**：设置页按「界面与外观 / 提醒策略 / 微信 / 飞书 / 邮件 / 系统」分组，左侧导航快速跳转。
- **自定义头像**：每个联系人可**上传照片**作头像（PNG/JPG/WEBP，≤2MB），未上传时回退到 emoji 头像。
- **纪念日模块**：新增「纪念日」类型（结婚 / 恋爱 / 入职 / 毕业…），同样支持提前提醒与「已携手 N 年」统计；「即将到来」页合并展示生日与纪念日。可在设置中关闭该入口。
- **MBTI / 血型字段**：联系人新增 MBTI 与血型；在列表 / 卡片 / 紧凑视图中带 ℹ️ 图标，**悬停显示性格与健康小贴士**。
- **GitHub Releases**：以 `vX.Y.Z` 形式发布版本，每次发版同时构建并推送版本固定镜像标签 `:vX.Y.Z`，便于锁定版本。

## v2.1 (2026-07-23)

联系人卡片化与视图增强：

- 列表 / 卡片 / 紧凑三种视图；按名称 / 倒计时 / 年龄 / 创建时间排序；按关系 / 性别 / 星座 / 生肖 / 历法 / 生日月份分组。
- 列表自动展示下次生日、倒计时、年龄、已活天数；「显示字段」选择器自定义展示列。
- 新增性别、出生时辰、星座、爱好、emoji 头像字段。

## v2.0 (2026-07-22)

现代化重构 + 多用户 + 全参数前台可调：

- 现代化简约 UI；多用户（管理员 / 普通用户）。
- 管理员可在网页「设置」页调整全部参数，**每项参数附说明**，保存即热生效、无需重启。
- 农历 / 公历双历法、微信 / 飞书 / 邮件多渠道通知。
- GitHub Actions 自动构建并发布到 GHCR，支持「零克隆零构建」直接拉镜像部署。
